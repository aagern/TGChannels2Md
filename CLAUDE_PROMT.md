Launch a team of 6 agents: AI, DEV, HABR, LINUXSEC, POLITICS, SCIENCE.

+ AI agent should analyze all files in only AI_NEWS subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders.
  
+ DEV agent should analyze all files in only DEV subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders. 

+ HABR agent should analyze all files in only HABR subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders.

+ LINUXSEC agent should analyze all files in only LINUXSEC subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders.

+ POLITICS agent should analyze all files in only POLITICS subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders.

+ SCIENCE agent should analyze all files in only SCIENCE subfolder and summarize them using rules.md instructions in this subfolder. Then it writes the results in <subfolder name>_<date>.md file in the parent NEWS folder. The agent must NOT look at files at any other folders.
